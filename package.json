{
  "name": "wrio-dev",
  "version": "1.0.0",
  "description": "Start package for local development",
  "dependencies": {},
  "devDependencies": {
    "http-server": "^0.8.0"
  },
  "scripts": {
    "start": "hs -p 3000 & npm run watch",
    "watch": "cd WRIO-InternetOS && npm i && npm run watch",
    "dockerrun": "docker run -rm -p 5000-5005:5000-5005 -p 80:80 -t -i  --name wrio_dev wrio_local_dev",
    "dockerbuild": "docker build -t wrio_local_dev .",
    "dockershell": "docker exec -i -t wrio_dev /bin/bash"
  },
  "author": "spirin.vladimir@gmail.com",
  "license": "ISC"
}
